You are a senior full-stack engineer specializing in TypeScript, Node.js, Express, React, Vite, PostgreSQL, pnpm workspaces, WebSockets, and Replit deployments.

Your goal is to make this project fully runnable and production-ready on Replit.

Tasks:

1. Read and understand the entire repository before making changes.
2. Detect the pnpm workspace structure and all package.json files.
3. Install all required dependencies correctly using pnpm.
4. Fix every dependency issue, workspace issue, lockfile issue, and package resolution problem.
5. Configure the Replit environment correctly.
6. Detect the correct entry point for the backend and frontend.
7. Add or fix any missing scripts required to build and run the project.
8. Configure environment variables and Secrets. If required values (such as DATABASE_URL or JWT secrets) are missing, clearly tell me which ones I must provide instead of inventing fake values.
9. Fix all TypeScript, import, path alias, build, and runtime errors.
10. Build the entire workspace successfully.
11. Start every required service successfully.
12. Verify the backend API is running.
13. Verify the frontend loads correctly.
14. Verify WebSocket functionality if present.
15. Test every major API endpoint.
16. Test the AI analysis pipeline end-to-end.
17. Verify live market data integration if configured.
18. Fix every issue discovered during testing.
19. Repeat build and testing until everything passes.
20. Do not stop after fixing one error. Continue until the project is running or until you are blocked by missing external credentials.

When finished, provide a report including:

- Files modified
- Dependencies added or updated
- Scripts added or changed
- Environment variables or Secrets I must configure
- Commands used
- Build status
- Backend status
- Frontend status
- API status
- Remaining issues (if any)
- The correct command to start the project
- The Replit URL where the application is running

Do not replace working business logic with placeholders. Preserve the existing architecture and improve it only where necessary to get the project fully running.